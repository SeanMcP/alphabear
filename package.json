{
  "name": "tl-starter",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "parcel build src/*.html --dist-dir docs/ --public-url /alphabear",
    "prebuild": "rm -rf docs/*.*",
    "prestart": "rm -rf docs/*.*",
    "start": "parcel src/*.html --dist-dir docs/"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.7.0",
    "parcel": "^2.7.0"
  },
  "dependencies": {
    "party-js": "^2.2.0"
  }
}
